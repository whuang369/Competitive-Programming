#include <bits/stdc++.h>
#define ll long long
using namespace std;
ll n, c;
void solve () {
    cin >> n >> c;
    ll ans = 0;
    for (ll x = 0; x <= )
}
int main () {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int T = 1;
    cin >> T;
    while (T --) {
        solve ();
    }
}