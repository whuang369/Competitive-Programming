/*
How many matches were played in a draw?
Determine which team has the most away wins. Print the name of the team.
Determine the team that has the most home defeats.
Determine at what time (hours-minutes) the most matches were played. The answer must be output in 𝐻𝐻
:𝑀𝑀
 format.
Determine the numbers of all matches for which the most goals were scored. It is necessary to display all numbers in ascending order separated by commas without spaces. For example, "1,2,3,4,5" (without quotes).
Determine the number of pairs of teams 𝑇1,𝑇2
 for which both matches played between these teams were won by the home team.
Print the name of the team that took last place.
Print the name of the team that became the champion (scored the most points).
Determine the number of teams that scored at least 50 goals in all matches.
Determine the number of teams that have more draws than wins.
*/
#include <bits/stdc++.h>
using namespace std;
void solve () {

}
int main () {
    // ios::sync_with_stdio(0);
    // cin.tie(0);
    // cout.tie(0);
    int T = 1;
    // cin >> T;
    while (T --) {
        solve ();
    }
}