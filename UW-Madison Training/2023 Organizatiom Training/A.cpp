#include <bits/stdc++.h>
using namespace std;

#ifdef local
#include<local/debug>
#else
#define dbg(...)
#endif


using i64 = long long;


void solve() {
    int a, b;
    cin >> a >> b;
    cout << a + b << "\n";
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    

    while (t--) {
        solve();
    }

    return 0;
}